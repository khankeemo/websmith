const { DashboardWidget } = require('./dashboard_widget');
const { SettingsWidget } = require('./settings_widget');
const { StatusWidget } = require('./status_widget');
const { ActivationButton } = require('./activation_button');

module.exports = { DashboardWidget, SettingsWidget, StatusWidget, ActivationButton };
